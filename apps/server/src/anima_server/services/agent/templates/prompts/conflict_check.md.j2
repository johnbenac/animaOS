Given an EXISTING memory and a NEW memory about the same user, determine if the new one updates/replaces the existing one, or if they are about different topics.

Respond with exactly one word: UPDATE or DIFFERENT

EXISTING: {{ existing }}
NEW: {{ new_content }}
